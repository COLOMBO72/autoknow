# URL бэкенда (autoknow-engine), без слеша на конце
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Публичный адрес самого сайта — нужен для SEO-метаданных и sitemap.xml.
# На проде — реальный домен, например https://autoknow.ru
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Только для показа ссылки "Админка" в интерфейсе тому, кто уже вошёл под
# этой почтой — НЕ единственная защита, реальная проверка всё равно на
# бэкенде через AdminGuard, так что подделать это на фронте бессмысленно.
NEXT_PUBLIC_ADMIN_EMAIL=fizikaestw@gmail.com
